<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" 
    "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="org.panjy.servicemetricsplatform.mapper.message.UserGuidanceStatMapper">
    
    <!-- 结果映射 -->
    <resultMap id="BaseResultMap" type="org.panjy.servicemetricsplatform.entity.message.UserGuidanceStat">
        <result column="create_time" jdbcType="DATE" property="createTime" />
        <result column="wechat_id" jdbcType="VARCHAR" property="wechatId" />
        <result column="guidance_count" jdbcType="INTEGER" property="guidanceCount" />
        <result column="personalized_guidance_count" jdbcType="INTEGER" property="personalizedGuidanceCount" />
    </resultMap>
    
    <!-- 基础字段列表 -->
    <sql id="Base_Column_List">
        create_time, wechat_id, guidance_count, personalized_guidance_count
    </sql>
    
    <!-- 批量查询用户指导统计记录 -->
    <select id="batchSelect" parameterType="java.util.List" resultMap="BaseResultMap">
        SELECT 
        <include refid="Base_Column_List" />
        FROM aikang.user_guidance_stat
        WHERE (create_time, wechat_id) IN
        <foreach collection="userGuidanceStats" item="item" open="(" separator="),(" close=")">
            #{item.createTime,jdbcType=DATE}, #{item.wechatId,jdbcType=VARCHAR}
        </foreach>
    </select>
    
    <!-- 批量插入用户指导统计记录 -->
    <insert id="batchInsert" parameterType="java.util.List">
        INSERT INTO aikang.user_guidance_stat (
            create_time, wechat_id, guidance_count, personalized_guidance_count
        ) VALUES
        <foreach collection="userGuidanceStats" item="item" separator=",">
            (
                #{item.createTime,jdbcType=DATE},
                #{item.wechatId,jdbcType=VARCHAR},
                #{item.guidanceCount,jdbcType=INTEGER},
                #{item.personalizedGuidanceCount,jdbcType=INTEGER}
            )
        </foreach>
    </insert>
    
    <!-- 根据微信ID和日期查询用户指导统计记录 -->
    <select id="selectByWechatIdAndDate" resultMap="BaseResultMap">
        SELECT 
        <include refid="Base_Column_List" />
        FROM aikang.user_guidance_stat
        WHERE wechat_id = #{wechatId,jdbcType=VARCHAR}
        AND create_time = #{createTime,jdbcType=DATE}
    </select>
    
    <!-- 插入用户指导统计记录 -->
    <insert id="insert" parameterType="org.panjy.servicemetricsplatform.entity.message.UserGuidanceStat">
        INSERT INTO aikang.user_guidance_stat (
            create_time, wechat_id, guidance_count, personalized_guidance_count
        ) VALUES (
            #{createTime,jdbcType=DATE},
            #{wechatId,jdbcType=VARCHAR},
            #{guidanceCount,jdbcType=INTEGER},
            #{personalizedGuidanceCount,jdbcType=INTEGER}
        )
    </insert>
    
    <!-- 更新用户指导统计记录 -->
    <update id="update" parameterType="org.panjy.servicemetricsplatform.entity.message.UserGuidanceStat">
        UPDATE aikang.user_guidance_stat
        SET 
            guidance_count = #{guidanceCount,jdbcType=INTEGER},
            personalized_guidance_count = #{personalizedGuidanceCount,jdbcType=INTEGER}
        WHERE wechat_id = #{wechatId,jdbcType=VARCHAR}
        AND create_time = #{createTime,jdbcType=DATE}
    </update>
    
    <!-- 获取总指导次数和总个性化指导次数 -->
    <select id="getTotalGuidanceCounts" resultMap="BaseResultMap">
        SELECT 
            SUM(guidance_count) AS guidance_count,
            SUM(personalized_guidance_count) AS personalized_guidance_count
        FROM aikang.user_guidance_stat
    </select>
    
    <!-- 获取指定月份的总指导次数和总个性化指导次数 -->
    <select id="getTotalGuidanceCountsByMonth" parameterType="java.time.LocalDateTime" resultMap="BaseResultMap">
        SELECT 
            SUM(guidance_count) AS guidance_count,
            SUM(personalized_guidance_count) AS personalized_guidance_count
        FROM aikang.user_guidance_stat
        WHERE toYear(create_time) = toYear(toDate(#{targetDate})) AND toMonth(create_time) = toMonth(toDate(#{targetDate}))
    </select>
    
</mapper>